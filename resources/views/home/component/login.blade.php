<div class="profile">
    <a href="{{ route('auth.register') }}">
        <button class="register">
            Đăng ký
        </button>
    </a>
    <a href="{{ route('auth.admin') }}">
        <button class="login">
            Đăng nhập
        </button>
    </a>
</div>
